/**
 * Service identity. Trivial real module so the build/lint/test pipeline has
 * something to compile and the ESM `.js`-suffix import path is exercised end to end.
 */
export const SERVICE_NAME = 'mailman';
export const SERVICE_VERSION = '0.1.0';
